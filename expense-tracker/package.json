{
  "name": "expense-tracker",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'Build completed' && exit 0",
    "start": "netlify dev"
  },
  "dependencies": {
    "faunadb": "^4.6.0",
    "netlify-identity-widget": "^1.9.0"
  },
  "devDependencies": {
    "netlify-cli": "^12.0.0"
  }
}
