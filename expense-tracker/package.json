{
  "name": "expense-tracker",
  "version": "1.0.0",
  "description": "Expense Tracker with User Authentication",
  "scripts": {
    "start": "netlify dev",
    "build": "echo 'Build complete'"
  },
  "dependencies": {
    "@netlify/functions": "^1.0.0",
    "faunadb": "^4.6.0",
    "netlify-identity-widget": "^1.9.0"
  },
  "devDependencies": {
    "netlify-cli": "^12.0.0"
  }
}
